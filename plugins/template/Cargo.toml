[package]
name = "template"
version = "0.1.0"
edition = "2021"
description = "Bundled Go-template, Helm, and Jinja2 language provider for mantis."
license = "GPL-3.0-or-later"
repository = "https://github.com/ansromanov/mantis"

[dependencies]
mantis = { path = "../.." }
serde_json = "1"
